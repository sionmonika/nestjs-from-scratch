services:
  typesense:
    image: typesense/typesense:29.0
    restart: on-failure
    ports:
      - "8900:8108"
    volumes:
      - ./typesense-data:/data
    command: '--data-dir /data --api-key=xyz --enable-cors'